plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "ma.elmasber.navegadorwpe"
    compileSdk = 35

    defaultConfig {
        applicationId = "ma.elmasber.navegadorwpe"
        minSdk = 31
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            minifyEnabled = false
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
            )
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }

    packagingOptions {
        jniLibs {
            useLegacyPackaging = true
        }
    }

    // Sin splits en v1: APK universal (ambos ABI). Se activan por ABI
    // cuando el verde esté confirmado.
}

dependencies {
    // Motor WebKit (WPE): SOLO dependencia externa precompilada.
    // Nada de este repo viene de Igalia/WebKit; ver NOTICES y docs/solo-lib.md.
    implementation("org.wpewebkit.wpeview:wpeview:0.3.3")
    implementation("org.khronos.openxr:openxr_loader_for_android:1.1.48")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.core:core-ktx:1.15.0")
}
