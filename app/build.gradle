plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "ma.elmasber.navegadorwpe"
    compileSdk = 35

    defaultConfig {
        applicationId = "ma.elmasber.navegadorwpe"
        minSdk = 24
        targetSdk = 35
        versionCode = 2
        versionName = "2.0-gecko"
    }

    buildTypes {
        release {
            minifyEnabled = false
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
            )
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    // Motor Gecko (Firefox): dependencia externa precompilada (MPL-2.0).
    // Nada de este repo viene de Mozilla; ver NOTICES.
    implementation("org.mozilla.geckoview:geckoview:155.0.20260826195058")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.core:core-ktx:1.15.0")
}
